<?xml version="1.0" encoding="utf-8"?>
<full-backup-content>
    <!-- Stored servers & users (without access tokens) -->
    <include
        domain="file"
        path="authentication_store.json" />

    <!-- UserPreferences -->
    <include
        domain="sharedpref"
        path="uk.rinzler.tv_preferences" />
    <include
        domain="sharedpref"
        path="uk.rinzler.tv.debug_preferences" />
    <!-- SystemPreferences -->
    <include
        domain="sharedpref"
        path="systemprefs" />
    <!-- AuthenticationPreferences -->
    <include
        domain="sharedpref"
        path="AuthenticationPreferences" />
    <!-- JellyseerrPreferences -->
    <include
        domain="sharedpref"
        path="jellyseerr_prefs" />
</full-backup-content>
